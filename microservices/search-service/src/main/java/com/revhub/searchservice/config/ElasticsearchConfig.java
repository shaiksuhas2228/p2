package com.revhub.searchservice.config;

import org.springframework.context.annotation.Configuration;

// Elasticsearch configuration disabled - install Elasticsearch to enable
@Configuration
public class ElasticsearchConfig {
    // Configuration disabled when Elasticsearch is not available
}